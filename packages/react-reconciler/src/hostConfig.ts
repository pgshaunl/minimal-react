/**
 * The `hostConfig` is used to provide host-specific methods for the reconciler.
 * For example, in the browser, we need to append the DOM elements to the DOM,
 * but in the server, we need to stringify the elements.
 */

export type Container = any;
